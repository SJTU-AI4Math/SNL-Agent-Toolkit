# Few-shot examples

_Empty. Populate with paired inputs (raw markdown / LaTeX / prose) and expected
outputs (`entries.json` + `graph.json` fragments) once we've run the first
spike with real data._

Naming convention (proposed): `NNN-<topic>/{input.md, expected.entries.json,
expected.graph.json, notes.md}`.
