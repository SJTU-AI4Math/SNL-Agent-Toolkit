{
  "format": "snl-entry",
  "version": 1,
  "schema_version": 1,
  "package": "CLI",
  "entry": {
    "id": "CLI.snl-macro--package-remove--member",
    "package": "CLI",
    "kind": "cli",
    "title": "snl macro-package remove-member",
    "content": {
      "markdown": "Use `snl macro-package remove-member` to remove one Macro from an existing Macro Package through guarded membership management.\n\n### Command path\n`macro-package/remove-member` is the canonical transport-neutral command. The shell adapter is `snl macro-package remove-member`; an Agent/MCP adapter submits the same operation object instead of reconstructing shell text.\n\n### Operation request\n```json\n{\"protocol\":\"snl.operation/v1\",\"command\":\"macro-package/remove-member\",\"root\":\"/absolute/workspace\",\"arguments\":{\"id\":\"<exact-package-id>\",\"member\":\"<exact-macro-id>\",\"expectedRevision\":\"<opaque>\"}}\n```\nEvery adapter accepts exactly this semantic request. Unknown argument keys are rejected. The CLI accepts `--root` and `--json` globally and emits one complete JSON document followed by a newline. Duplicate add, absent remove, wrong owner, or stale revision is a conflict with unchanged state.\n\n### Success result\nA successful call exits `0` and returns `{\"protocol\":\"snl.result/v1\",\"ok\":true,\"command\":\"macro-package/remove-member\",\"data\":...,\"diagnostics\":[]}`. `data` contains package and member canonical readbacks plus resulting revisions.\n\n### Failure and exit semantics\nDomain rejection (invalid data, not-found, conflict, or validation errors) exits `1`. Invocation, input I/O, unsupported workspace/schema, lock acquisition, and runtime failures exit `2`. Failure stdout under `--json` is exactly one `{\"protocol\":\"snl.result/v1\",\"ok\":false,\"command\":\"macro-package/remove-member\",\"error\":{\"code\":\"<stable-code>\",\"message\":\"<actionable text>\",\"details\":...,\"retryable\":false}}`; no stack trace or prose contaminates stdout.\n\n### State and concurrency\nMutating. Change package membership and member ownership together under one lock with guarded rollback. Revisions, cursors, checked digests, and workspace revisions are opaque tokens. After success, canonical readback and whole-workspace validation must observe the published state.\n"
    },
    "contribution_info": null,
    "pointer": null
  }
}
