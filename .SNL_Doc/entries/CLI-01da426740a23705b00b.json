{
  "format": "snl-entry",
  "version": 1,
  "schema_version": 1,
  "package": "CLI",
  "entry": {
    "id": "CLI.snl-relationship-update",
    "package": "CLI",
    "kind": "cli",
    "title": "snl relationship update",
    "content": {
      "markdown": "Use `snl relationship update` to replace the mutable fields of an existing Relationship under revision control.\n\n### Command path\n`relationship/update` is the canonical transport-neutral command. The shell adapter is `snl relationship update`; an Agent/MCP adapter submits the same operation object instead of reconstructing shell text.\n\n### Operation request\n```json\n{\"protocol\":\"snl.operation/v1\",\"command\":\"relationship/update\",\"root\":\"/absolute/workspace\",\"arguments\":{\"id\":\"<exact-id>\",\"value\":{},\"expectedRevision\":\"<opaque>\"}}\n```\nEvery adapter accepts exactly this semantic request. Unknown argument keys are rejected. The CLI accepts `--root` and `--json` globally and emits one complete JSON document followed by a newline. Stale revision is a conflict (exit 1) with unchanged bytes.\n\n### Success result\nA successful call exits `0` and returns `{\"protocol\":\"snl.result/v1\",\"ok\":true,\"command\":\"relationship/update\",\"data\":...,\"diagnostics\":[]}`. `data.entity` is the canonical readback with a new revision.\n\n### Failure and exit semantics\nDomain rejection (invalid data, not-found, conflict, or validation errors) exits `1`. Invocation, input I/O, unsupported workspace/schema, lock acquisition, and runtime failures exit `2`. Failure stdout under `--json` is exactly one `{\"protocol\":\"snl.result/v1\",\"ok\":false,\"command\":\"relationship/update\",\"error\":{\"code\":\"<stable-code>\",\"message\":\"<actionable text>\",\"details\":...,\"retryable\":false}}`; no stack trace or prose contaminates stdout.\n\n### State and concurrency\nMutating. Under the shared lock, reread and compare `expectedRevision`, preserve unknown fields represented by the replacement, reject identity changes, validate, durably replace, and read back. Revisions, cursors, checked digests, and workspace revisions are opaque tokens. After success, canonical readback and whole-workspace validation must observe the published state.\n"
    },
    "contribution_info": null,
    "pointer": null
  }
}
