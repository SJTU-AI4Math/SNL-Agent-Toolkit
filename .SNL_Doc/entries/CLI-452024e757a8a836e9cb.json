{
  "format": "snl-entry",
  "version": 1,
  "schema_version": 1,
  "package": "CLI",
  "entry": {
    "id": "CLI.snl-relationship-generate",
    "package": "CLI",
    "kind": "cli",
    "title": "snl relationship generate",
    "content": {
      "markdown": "Use `snl relationship generate` to regenerate derived relationships from current authoritative entities.\n\n### Availability\nThis Entry is a normative product contract. The command is implemented only when its canonical path appears in machine-readable `snl --help`; absence means planned and unavailable, never silently emulated through a different command.\n\n\n### Command path\n`relationship/generate` is the canonical transport-neutral command. The intended shell spelling is `snl relationship generate`. Current availability is advertised by machine-readable `snl --help`; an Agent/MCP adapter submits the same operation object instead of reconstructing shell text.\n\n### Operation request\n```json\n{\"protocol\":\"snl.operation/v1\",\"command\":\"relationship/generate\",\"root\":\"/absolute/workspace\",\"arguments\":{\"scope\":{},\"expectedWorkspaceRevision\":\"<opaque>\",\"dryRun\":false}}\n```\nEvery adapter accepts exactly this semantic request. Unknown argument keys are rejected. The CLI accepts `--root` and `--json` globally and emits one complete JSON document followed by a newline. Never overwrite manual relationships or publish derivation from a stale snapshot.\n\n### Success result\nA successful call exits `0` and returns `{\"protocol\":\"snl.result/v1\",\"ok\":true,\"command\":\"relationship/generate\",\"data\":{},\"diagnostics\":[]}`. `data` contains deterministic derived relationships, additions/removals, provenance, and resulting revision when applied.\n\n### Failure and exit semantics\nDomain rejection (invalid data, not-found, conflict, or validation errors) exits `1`. Invocation, input I/O, unsupported workspace/schema, lock acquisition, and runtime failures exit `2`. Failure stdout under `--json` is exactly one `{\"protocol\":\"snl.result/v1\",\"ok\":false,\"command\":\"relationship/generate\",\"error\":{\"code\":\"<stable-code>\",\"message\":\"<actionable text>\",\"details\":{},\"retryable\":false}}`; no stack trace or prose contaminates stdout.\n\n### State and concurrency\nDry-run derives only. Apply binds the reviewed workspace snapshot under the shared lock and replaces only the managed derived relationship slice. Revisions, cursors, checked digests, and workspace revisions are opaque tokens. After success, canonical readback and whole-workspace validation must observe the published state.\n\n\n### Established cache-only direction; publication integration pending\nThe product direction is global, rebuildable cache-only generation; no new owner choice between Authoring writes and caches is required. Apply must preserve all Authoring bytes, including historical automatic rows. Only the current composed view replaces the managed derived slice; additions/removals describe that view, not saved-row deletion. The empty scope uses complete workspace Entry id+SNL, active Macro name+source.entries and authored relationships; never shrink input to a Package/Library/UI selection or invent scoped selector fields.\n\nReuse pinned Extension 76acedbc `spec.cache`, `spec.cache.runtime`, `spec.cache.dependencies` and `derivedCache.cachePath`: `.SNL_Doc/.cache/dependencies/result.json`, generator `dependencies`, algorithm version `1`, existing `snl-derived-cache` schema `1`, global `library:null`, complete canonical inputHash and valueHash. This is not a new Authoring/cache schema. Dry-run must be memory-only and create neither cache nor lock residue. Apply must reuse the shared lock and fresh Authoring CAS, recheck complete input before publication, report actual publication failure (not success from memory fallback), read back using the same complete input and validate unchanged Authoring. An unchanged resulting Authoring revision is valid for cache-only publication.\n\nThe Authoring revision prerequisite now excludes exactly reserved global and legal Library cache subtrees; unknown author assets and cache-like paths elsewhere remain authority. Physical batch copying/readback/rollback remains separate and includes caches, with possible physical-conflict retry or retained recovery during concurrent cache churn. Command wiring, persistent cache publish/readback, transport and mixed-writer acceptance remain engineering work. Until `relationship/generate` appears in machine help it is unavailable; the pure SDK is not a publisher. Do not restore the legacy Authoring writer.\n"
    },
    "contribution_info": null,
    "pointer": null
  }
}
