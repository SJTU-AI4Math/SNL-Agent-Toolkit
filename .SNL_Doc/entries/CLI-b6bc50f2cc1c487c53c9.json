{
  "format": "snl-entry",
  "version": 1,
  "schema_version": 1,
  "package": "CLI",
  "entry": {
    "id": "CLI.snl-entry--kind-get",
    "package": "CLI",
    "kind": "cli",
    "title": "snl entry-kind get",
    "content": {
      "markdown": "Use `snl entry-kind get` to retrieve one exact Entry Kind and its current opaque revision.\n\n### Command path\n`entry-kind/get` is the canonical transport-neutral command. The shell adapter is `snl entry-kind get`; an Agent/MCP adapter submits the same operation object instead of reconstructing shell text.\n\n### Operation request\n```json\n{\"protocol\":\"snl.operation/v1\",\"command\":\"entry-kind/get\",\"root\":\"/absolute/workspace\",\"arguments\":{\"id\":\"<exact-id>\"}}\n```\nEvery adapter accepts exactly this semantic request. Unknown argument keys are rejected. The CLI accepts `--root` and `--json` globally and emits one complete JSON document followed by a newline. Missing identity is a domain not-found error (exit 1), distinct from unreadable workspace failure (exit 2).\n\n### Success result\nA successful call exits `0` and returns `{\"protocol\":\"snl.result/v1\",\"ok\":true,\"command\":\"entry-kind/get\",\"data\":...,\"diagnostics\":[]}`. `data.entity` contains exact type, identity, complete value including unknown fields, and opaque revision.\n\n### Failure and exit semantics\nDomain rejection (invalid data, not-found, conflict, or validation errors) exits `1`. Invocation, input I/O, unsupported workspace/schema, lock acquisition, and runtime failures exit `2`. Failure stdout under `--json` is exactly one `{\"protocol\":\"snl.result/v1\",\"ok\":false,\"command\":\"entry-kind/get\",\"error\":{\"code\":\"<stable-code>\",\"message\":\"<actionable text>\",\"details\":...,\"retryable\":false}}`; no stack trace or prose contaminates stdout.\n\n### State and concurrency\nRead-only and stateless. Revisions, cursors, checked digests, and workspace revisions are opaque tokens. Repeated calls against unchanged bytes return equivalent semantic results.\n"
    },
    "contribution_info": null,
    "pointer": null
  }
}
